{
  "name": "roma-finanzas",
  "version": "2.1.3",
  "description": "PWA y APK Android para Roma Finanzas",
  "main": "app.js",
  "scripts": {
    "build:web": "node scripts/build-web.js",
    "preview": "npm run build:web && node scripts/serve-dist.js",
    "test": "node --test tests/*.test.js",
    "verify:build": "node scripts/verify-build.js",
    "db:standalone:bundle": "node scripts/build-standalone-sql.js",
    "db:federated:bundle": "node scripts/build-federated-target-sql.js",
    "pilot:preflight": "node scripts/pilot-preflight.js",
    "verify:federated": "node scripts/verify-federated-function.js",
    "check": "npm run test && npm run build:web && npm run verify:build",
    "cap:prepare": "npm run build:web && powershell -ExecutionPolicy Bypass -File scripts/prepare-capacitor.ps1",
    "android:add": "npm run cap:prepare && npx cap add android",
    "android:sync": "npm run cap:prepare && npx cap sync android && npm run android:icons",
    "android:open": "npm run android:sync && npx cap open android",
    "apk:debug": "npm run android:sync && powershell -ExecutionPolicy Bypass -File scripts/build-apk-debug.ps1",
    "android:icons": "powershell -ExecutionPolicy Bypass -File scripts/apply-android-icons.ps1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tusalon/RomaFinanzas.git"
  },
  "keywords": [
    "rservasroma",
    "finanzas",
    "pwa",
    "android"
  ],
  "author": "RservasRoma",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/tusalon/RomaFinanzas/issues"
  },
  "homepage": "https://github.com/tusalon/RomaFinanzas#readme",
  "devDependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "@supabase/supabase-js": "^2.112.0",
    "@tailwindcss/cli": "^4.3.3",
    "esbuild": "^0.28.1",
    "lucide-static": "^1.17.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwindcss": "^4.3.3"
  }
}
